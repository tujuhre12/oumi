# Phi3-Vision DPO train config.
#
# Usage:
#   oumi train -c configs/recipes/vision/phi3/dpo/train.yaml
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/train/train.html
#   - Config class: oumi.core.configs.TrainingConfig
#   - Config source: https://github.com/oumi-ai/oumi/blob/main/src/oumi/core/configs/training_config.py
#   - Other training configs: configs/**/*train.yaml

model:
  model_name: "microsoft/Phi-3-vision-128k-instruct"
  trust_remote_code: True
  attn_implementation: "eager"

data:
  train:
    datasets:
      - dataset_name: "vision_dpo_jsonl"
        dataset_path: "data/dataset_examples/vision_language_dpo_format.jsonl"
        dataset_kwargs:
          processor_name: "microsoft/Phi-3-vision-128k-instruct"
          return_tensors: True
        # Example format:
        # {
        #   "prompt": "What is shown in this image?",
        #   "images": ["path/to/image.jpg"],
        #   "chosen": [{"role": "assistant", "content": "This image shows a well-described accurate response."}],
        #   "rejected": [{"role": "assistant", "content": "This image shows an inaccurate or poor response."}]
        # }

training:
  optimizer: "adamw_torch"
  use_peft: false
  output_dir: "output/phi3_vision.vision_dpo"
  trainer_type: "TRL_DPO"
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 16
  learning_rate: 5e-6
  warmup_steps: 100
  max_steps: 1000
  logging_steps: 10
  save_steps: 500
  eval_steps: 500

peft:
  q_lora: False
  lora_target_modules:
    - "q_proj"
    - "k_proj"
    - "v_proj"
    - "o_proj"
    - "gate_proj"
    - "up_proj"
    - "down_proj"
  lora_r: 16
  lora_alpha: 32
  lora_dropout: 0.1
